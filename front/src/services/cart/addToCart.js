import axios from "axios";
import { BASE_ROUTE } from "../../config.js";

export const addToCart = async (total, shoppingCartId, nickname) => {
  try {
    // TODO
    return createdCart;
  } catch (error) {
    console.log("addToCart service error", error);
  }
};
